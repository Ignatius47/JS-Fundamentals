const numOccurrences = process.argv[2];
const X = parseInt(numOccurrences);
if (isNaN(X)) {
  console.log('Not a number');
} else {
let i = 0;
while (i < X) {
    console.log('C is fun');
    i++;
}
}